# План: удалить Docker-тесты

## 1. Удалить файлы

- `testsDocker/` — папка с тестами целиком
- `test-docker.sh` — скрипт запуска

## 2. Обновить package.json

Удалить скрипт `"test:docker"`.

## 3. Убрать упоминания из документации

Проверить `README.md` и `CLAUDE.md` на упоминания Docker-тестов и `test-docker.sh` — удалить если есть.

## Файлы для изменения

| Действие | Файл                 |
| -------- | -------------------- |
| Удалить  | `testsDocker/`       |
| Удалить  | `test-docker.sh`     |
| Изменить | `package.json`       |
| Проверить | `README.md`         |
| Проверить | `CLAUDE.md`         |
