{
  "name": "webpush-scheduler",
  "type": "module",
  "scripts": {
    "setup": "npm ci && npm run keys",
    "keys": "web-push generate-vapid-keys",
    "start": "node --env-file .env src/main.ts",
    "dev": "node --env-file .env --watch src/main.ts",
    "test": "node --test tests/**/*.test.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^24.6.2"
  },
  "engines": {
    "node": ">= 22.6.0"
  }
}