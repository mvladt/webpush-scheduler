{
  "name": "webpush-scheduler",
  "type": "module",
  "scripts": {
    "dev": "node --env-file .env --watch src/main.ts",
    "test": "node --test tests/**/*.test.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^24.6.2"
  }
}